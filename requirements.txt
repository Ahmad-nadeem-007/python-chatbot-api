fastapi
uvicorn
pydantic
pydantic-settings
python-dotenv

# PostgreSQL
sqlalchemy[asyncio]
asyncpg

# AI
langchain-groq

# RAG (Knowledge Base)
chromadb              # Local vector database (no Postgres extension required)
sentence-transformers # Free/local model that converts text into embeddings (numbers)
